extend type Mutation {
  sendMail(
    name: String!
    email: String!
    subject: String!
    message: String!
  ): SendMailResponse!
}

type SendMailResponse {
  id: String!
  # accepted: [String]! | [MailAddress]!
  # rejected: [String]! | [MailAddress]!
  # pending: [String]! | [MailAddress]!
  # response: String!
}
